{
    "name": "Blog",
    "description": "基于VuePress搭建的博客、托管于Github Pages、由Travis CI提供持续集成服务",
    "scripts": {
        "dev": "vuepress dev src",
        "build": "vuepress build src",
        "deploy": "~/bin/deploy.sh",
        "deploy-ci": "./deploy.sh",
        "clean": "rm -rf dist"
    },
    "license": "MIT",
    "devDependencies": {
        "@vuepress/plugin-back-to-top": "^1.0.0-rc.1",
        "@vuepress/plugin-pwa": "^1.0.0-rc.1",
        "vuepress": "^1.0.0-rc.1"
    },
    "dependencies": {
        "moment": "^2.24.0"
    }
}
